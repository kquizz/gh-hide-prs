{
  "manifest_version": 3,
  "name": "GitHub Hidden-PR Filter",
  "version": "1.2.0",
  "description": "Hides PRs and issues from a repo's Pull requests / Issues tabs (link + count) by label, author, or draft status. Works on any GitHub repo.",
  "permissions": ["storage"],
  "action": {
    "default_popup": "popup.html",
    "default_title": "GitHub Hidden-PR Filter settings"
  },
  "content_scripts": [
    {
      "matches": [
        "https://github.com/*"
      ],
      "js": ["content.js"],
      "run_at": "document_start"
    }
  ]
}
